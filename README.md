# Warehouse-Systems-Template
system, with frontend &amp; backend server記錄入庫出庫的order,actual record,&amp; check inventory.  The records are per customer. Inventory with model and quantity. Each product model has a flag, need import/export permit or not (need to upload pdf/image if needed), and each in/out/record also attach multiple photos. 
